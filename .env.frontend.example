# Copy into .env.local at the repo root (Vite reads from there). See README for Supabase setup.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional — only if the API is not same-origin (e.g. static host + API on another domain)
# VITE_API_BASE=
