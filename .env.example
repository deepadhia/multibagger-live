# Copy to .env.local at the repo root. See README for migrations, admin seed, and optional features.
# Frontend (Vite): use .env.frontend.example for the VITE_* variables.

# Backend (Express)
DATABASE_URL=
JWT_SECRET=

# Optional
# PORT=4000
