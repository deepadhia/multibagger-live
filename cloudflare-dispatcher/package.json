{
  "name": "multibagger-workflow-dispatcher",
  "version": "1.0.0",
  "description": "Cloudflare Worker to trigger GitHub Action workflows on cron schedule",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
